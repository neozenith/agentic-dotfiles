# T1.1: repeat lookups are served from the cache

> - **Gap:** [G1: Cache the widget lookup path](./G1.md)
> - **Index:** [README.md](./README.md)

- [ ] **Done**

Calling `lookup_widget("w3")` twice yields equal results and
`lookup_widget.cache_info().hits >= 1` after the second call. _(tracer bullet)_

| | |
|--|--|
| Test | `tests/test_cache.py::test_repeat_lookup_hits_cache` — two identical lookups through the public interface; assert equal results and `cache_info().hits >= 1` |
| Implements | `src/widgets.py` `lookup_widget` — real cache on the shipped seam, never a stub |
| Depends on | — |
