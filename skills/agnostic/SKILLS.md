---
name: agnostic
description: Keep documentation generic by renaming project-, client-, or company-specific names, entities, palettes, and paths to generic open-source-style placeholders. Use when authoring or editing any documentation, even when a project-specific example instigated the change, so the wording is generalised before commit. Also use when the user asks to "make this generic", "de-brand", "anonymise the docs", or "check for leaked names". Skip for project source code, tests, and app-facing docs that are meant to reference the real project.
user-invocable: true
---


# Agnostic Documentation

Documentation must remain generic.

There might be a project specific example that instigates the creation or update of these files but they MUST NOT use project specific names and rename entities to be generic in nature like open source documentation.