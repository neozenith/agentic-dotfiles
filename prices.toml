# Project-scope price rows for pytest-xharness-eval. Rows here add to or override the
# plugin's bundled table (ini key `xharness_prices`, default: this file).
# USD per token. Public per-MTok rates divided by 1e6.

["gpt-5.6-terra"]
# Codex alias with no public row; assumed GPT-5-class, same as the bundled sol/luna rows.
# Verify against the provider before trusting a sweep's absolute USD.
input       = 1.25e-6
output      = 1.0e-5
cache_read  = 1.25e-7
cache_write = 1.25e-6
